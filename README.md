# 📄 Article Uploading System

**Article Uploading System** is an ASP.NET–based web application designed for uploading and managing academic articles or content. It allows users to upload article files, view them, and manage stored content efficiently.

---

## 🚀 Features

✅ Web application developed using ASP.NET Core MVC  
✅ Article upload functionality (file upload)  
✅ Uploaded articles are stored in the database  
✅ Model-View-Controller (MVC) architecture  
✅ Clean and easy-to-understand code structure  

---

## 🧱 Project Structure

The project includes the following main folders:

Controllers/ → Application logic
Data/ → Database access and data operations
Models/ → Entity and ViewModel classes
Services/ → Business logic services
Views/ → Razor views
wwwroot/ → Static files (CSS, JavaScript, etc.)
---

## 🛠️ Requirements

To run this project, the following tools are required:

- .NET SDK (6.0 or higher)
- Visual Studio **or** VS Code
- SQL Server (with the included `makale.db` database file)

---

## ▶️ Run the Project

Open the project in Visual Studio or run it via the terminal:

```bash
dotnet restore
dotnet build
dotnet run
